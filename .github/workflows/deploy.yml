name: Build Tailwind CSS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-css:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 4️⃣ Create public/styles folder
      - name: Create output folder
        run: mkdir -p public/styles

      # 5️⃣ Build Tailwind CSS
      - name: Build Tailwind CSS
        run: npx @tailwindcss/cli -i ./src/input.css -o ./public/styles/main.css --minify

      # 6️⃣ Optional: show filesize
      - name: Show CSS file size
        run: ls -lh public/styles/main.css
